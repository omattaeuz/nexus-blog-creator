{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm test:ci",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}